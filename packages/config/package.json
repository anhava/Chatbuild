{
  "name": "@repo/config",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "tsup": "^6.0.1",
    "typescript": "^5.0.0"
  }
}
